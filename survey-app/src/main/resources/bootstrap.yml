spring:
  application:
    name: survey-app
  profiles:
    active:
    - ${ACTIVE_PROFILE}  
  cloud:
    config:
      uri:
      - ${CONFIG_SERVER_URL}
      
server:  port: ${SERVER_PORT}